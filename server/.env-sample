# DO NOT EDIT THIS FILE DIRECTLY
# Copy this file to .env and enter your own values
#
# INWORLD_API_KEY is required
INWORLD_API_KEY=
# VAD_MODEL_PATH is optional, defaults to packaged https://github.com/snakers4/silero-vad
VAD_MODEL_PATH=
# LLM_MODEL_NAME is optional, defaults to `gpt-4o-mini`
LLM_MODEL_NAME=
# LLM_PROVIDER is optional, defaults to `openai`
LLM_PROVIDER=
# VOICE_ID is optional, defaults to `Dennis`
VOICE_ID=
# TTS_MODEL_ID is optional, defaults to `inworld-tts-1`
TTS_MODEL_ID=
# If enabled, it will be saved in system tmp folder.
# Path will be printed in CLI on application start.
# Default value is `false`, set `true` to enable this feature
GRAPH_VISUALIZATION_ENABLED=
# STT (Speech-to-Text) Configuration
# These settings determine the DEFAULT STT service used at startup
# For dynamic runtime selection via UI, provide all relevant API keys

# USE_ASSEMBLY_AI is optional, set to `true` to use Assembly.AI STT as default
USE_ASSEMBLY_AI=
# ASSEMBLY_AI_API_KEY - provide to enable Assembly.AI (required if USE_ASSEMBLY_AI=true)
ASSEMBLY_AI_API_KEY=

# USE_GROQ is optional, set to `true` to use Groq Whisper STT as default
USE_GROQ=
# GROQ_API_KEY - provide to enable Groq Whisper (required if USE_GROQ=true)
GROQ_API_KEY=
# GROQ_MODEL is optional, defaults to `whisper-large-v3-turbo` (faster and cheaper)
# Options: `whisper-large-v3-turbo` (fast, $0.04/hr) or `whisper-large-v3` (more accurate, $0.111/hr)
GROQ_MODEL=

# Note: If neither USE_ASSEMBLY_AI nor USE_GROQ is true, defaults to Inworld Remote STT
# To support dynamic STT selection in UI, provide API keys for all services you want to enable