# DO NOT EDIT THIS FILE DIRECTLY
# Copy this file to .env and enter your own values
#

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Inworld API Key - Required
# Get your API key from https://platform.inworld.ai/
INWORLD_API_KEY=""

# Assembly.AI API Key - Required for audio transcription
# Get your API key from https://www.assemblyai.com/
ASSEMBLYAI_API_KEY=""

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# LLM Model Configuration (Fallback)
# These settings define the default/fallback model used when no model_id is specified
# in the session.update event. Individual sessions can override this via model_id and
# model_selection fields in their session configuration.
# DEFAULT_LLM_PROVIDER_NAME: Fallback LLM provider (default: 'google')
#   Options: 'google', 'openai', 'anthropic', 'groq', etc.
# DEFAULT_LLM_MODEL_NAME: Fallback model name (default: 'gemini-2.5-flash')
#   Examples: 'gemini-2.5-flash', 'gpt-4o', 'claude-3-sonnet', 'llama-3.3-70b-versatile', etc.
#DEFAULT_LLM_PROVIDER_NAME=
#DEFAULT_LLM_MODEL_NAME=

# Voice and TTS Configuration
# VOICE_ID: Voice ID for TTS (default: 'Dennis')
# TTS_MODEL_ID: TTS model ID (default: 'inworld-tts-1')
#VOICE_ID=
#TTS_MODEL_ID=

# VAD (Voice Activity Detection) Model Path
# Optional: Path to custom VAD model
# Defaults to packaged model from https://github.com/snakers4/silero-vad
#VAD_MODEL_PATH=

# Graph Visualization
# If enabled, graph visualizations will be saved in system tmp folder.
# Path will be printed in CLI on application start.
# Default: false (set to 'true' to enable)
#GRAPH_VISUALIZATION_ENABLED=false

# Disable Auto-Interruption
# Disable auto-interruption feature (graph-based interruptions)
# Default value is `false` (auto-interruptions enabled), set `true` to disable
#DISABLE_AUTO_INTERRUPTION=false

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# AUTH_TOKEN: Optional WebSocket authentication token
# If set, clients must provide this token as a query parameter: ?token=YOUR_TOKEN
# Note: This is checked in the WebSocket upgrade request query params, not headers
# Recommended for basic security, especially in production
#AUTH_TOKEN=

# =============================================================================
# APPLICATION METADATA (Optional)
# =============================================================================

# Application Name: Used in logging and telemetry (default: 'realtime-service')
#APP_NAME=realtime-service

# Application Version: Used in logging and telemetry (default: '1.0.0')
#APP_VERSION=1.0.0

# Workspace ID: Used for multi-tenant scenarios (default: 'default')
# Can also be provided via 'workspace-id' header in WebSocket requests
#WORKSPACE_ID=default

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# REALTIME_LOG_LEVEL: Log level ('debug', 'info', 'warn', 'error')
# Default: 'info'
REALTIME_LOG_LEVEL="info"

# REALTIME_LOG_PRETTY: Enable pretty-printed logs for local development
# Set to '1' for pretty logs, '0' or unset for JSON logs (cloud-ready)
# Default: '0' (JSON format)
# Examples:
#   npm start                              # JSON logs (default, cloud-ready)
#   REALTIME_LOG_PRETTY=1 npm start        # Pretty logs (local dev)
#   REALTIME_LOG_LEVEL=debug npm start     # JSON with debug level
REALTIME_LOG_PRETTY=1

# =============================================================================
# SERVER PORTS (Hardcoded in config, shown for reference)
# =============================================================================
# WebSocket Server Port: 4000 (configured in src/config.ts)
# Metrics Server Port: 9000 (configured in src/index.ts)
# To change ports, modify the source code configuration files.
